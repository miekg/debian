#!/bin/sh

set -e

case "$1" in
    configure)
        if ! getent passwd prometheus > /dev/null; then
            adduser --system --disabled-password --disabled-login --home /var/lib/prometheus \
                    --quiet --force-badname --group prometheus
        fi
        ;;
esac

#DEBHELPER#

exit 0
