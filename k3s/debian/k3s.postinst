#!/bin/sh

set -e

case "$1" in
    configure)
        if ! getent passwd k3s > /dev/null; then
            adduser --system --disabled-password --disabled-login --home /var/lib/k3s \
                    --quiet --force-badname --group k3s
        fi
        ;;
esac

#DEBHELPER#

exit 0
